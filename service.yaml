apiVersion: v1
kind: Service
metadata:
  name: myapi-service
spec:
  selector:
    app: myapi
  type: NodePort
  ports:
    - protocol: TCP
      port: 80       # Internal port
      targetPort: 80 # Container port
      nodePort: 30080 # External port (must be 30000-32767)
